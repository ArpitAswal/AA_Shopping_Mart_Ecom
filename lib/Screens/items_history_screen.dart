import 'package:flutter/material.dart';

class ItemsHistoryScreen extends StatelessWidget {
  const ItemsHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold());
  }
}
